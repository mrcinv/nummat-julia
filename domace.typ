#import "admonitions.typ": opomba

= Domače naloge
#set heading(offset: 1)

#include "domace_seznam.typ"

#opomba(naslov: [Domačo nalogo lahko delate skupaj s kolegi])[
Domačo nalogo lahko delate skupaj s kolegi, vendar morate v tem primeru rešiti toliko različnih nalog, kot je študentov v skupini.
]

#show outline: set heading(level: 2)

#include "domace_01.typ"
#include "domace_02.typ"
#include "domace_03.typ"

#set heading(offset: 0)
